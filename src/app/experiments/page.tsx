import { NotAvailableYet } from "@/components/ui/States";

export default function ExperimentsPage() {
  return (
    <NotAvailableYet
      feature="Experiments"
      description="Experiments group AutoML runs. They arrive with the AutoML training phase (Phase 8–11 of the roadmap)."
    />
  );
}
