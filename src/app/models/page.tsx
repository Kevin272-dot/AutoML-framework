import { NotAvailableYet } from "@/components/ui/States";

export default function ModelsPage() {
  return (
    <NotAvailableYet
      feature="Models"
      description="Trained model artifacts, metrics, and model detail pages arrive with the AutoML training phase."
    />
  );
}
